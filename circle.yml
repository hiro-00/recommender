machine:
    python:
        version: 3.4.3

dependencies:
    pre:
        - pip install -r ./requirements.txt
        - export PYTHONPATH=${PYTHONPATH}:${HOME}/rec/rec

test:
    override:
        - pytest